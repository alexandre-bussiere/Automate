//
// Created by Albane Coiffe on 13/03/2023.
//

#include "complet.h"
#include <stdbool.h>

bool IsComplet (listEtat automate ) {
    bool comlet = true,
    while ((nombre.next != NULL) && (complet != false) ){
        int i =0;
        while ((automate.data->listnbliaison[i] != 0) &&()){
            i++;
        }
    }
}